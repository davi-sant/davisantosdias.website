import { styled } from '@stitches';

export const Text = styled("p", {
  fontFamily: "$DM",


  variants: {
    variant: {
      textHeader: {
        fontFamily: "$IBM",
        fontSize: "$1",
      }
    }
  }
})
