import { styled } from "../../stitches.config";

const I = styled('i', {
  
  variants: {
    variant: {
      iconCaffe: {
        color: '$color03',
        margin: '0 5px',
      }
    }
  }
})
export default I;
