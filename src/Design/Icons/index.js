import { styled } from "../../stitches.config";

export const I = styled('i', {

  variants: {
    variant: {
      iconCaffe: {
        color: '$color03',
        margin: '0 5px',
      }
    }
  }
})
